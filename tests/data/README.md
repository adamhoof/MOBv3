# Test Data

`catalog_5_products.mdb` is a tiny Access MDB fixture used by integration tests.

It contains one `SKz` table with five products and the columns required by
`source/cmd/catalog_service/mdb_to_csv.sh`.
